#!/usr/bin/make -f
# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

include /usr/share/cdbs/1/rules/debhelper.mk

build/symbiosis-updater::
	make manpage

cleanbuilddir/symbiosis-updater::
	make clean

