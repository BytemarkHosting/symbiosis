  #
  # Used by both the Sieve plugin and the ManageSieve protocol.  This is the
  # *default* value, and overridden in Symbiosis by the values returned from
  # the dict proxy.
  #
  sieve      = file:~/sieve;active=~/sieve.d

