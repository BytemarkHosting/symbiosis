#
#  The file we scan for patterns
#
file = /var/log/exim4/rejectlog

#
#  The patterns we use
#
\[__IP__\] F=<[^>]+> rejected RCPT <[^>]+>: Relaying not permitted
authenticator failed for [^ ]+ \(.*\) \[__IP__\]: 535 Incorrect authentication data
